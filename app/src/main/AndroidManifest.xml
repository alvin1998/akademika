<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.AdaInt.akademika">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.mahasiswa.DasboardFragmentMhs"></activity>
        <activity android:name=".activity.mahasiswa.MainActivityFragmenMhs" />
        <activity android:name=".activity.login.Register" />
        <activity android:name=".activity.login.ActivityLogin" />
        <activity android:name=".activity.dosen.MainActivityFragment" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>